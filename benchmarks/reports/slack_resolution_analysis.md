# Slack-resolution penalty analysis

This report records the analysis slice for [issue #208](https://github.com/JuliaQUBO/ToQUBO.jl/issues/208). It tests the continuous-slack mechanism separately from its proposed causal role in [issue #205](https://github.com/JuliaQUBO/ToQUBO.jl/issues/205).

## Provenance and scope

- ToQUBO base commit: `ed438ebe8ad6d39873c52894ddcc25a2825cb62c`
- Public fixture: generated by `benchmarks/slack_resolution_analysis.jl`
- Canonical #205 model: access-controlled `ZOMEGA-Group/QUBO_UC`, branch `QUBO_feasibility_tests`, commit `707b30a527d33da3df0cdeb3ca336d55a28ca90a`
- Canonical source copied into this repository: no
- Julia: 1.10.11; JuMP: 1.30.1; ToQUBO: 0.6.0 from this checkout
- Local sampler: Neal 0.6.0 with dimod 0.12.21
- Sampler design: seeds 1-10, 10,000 reads per seed, 100 sweeps

Run the public portion with:

```sh
JULIA_LOAD_PATH="@:test:@stdlib" julia +1.10 --project=. benchmarks/slack_resolution_analysis.jl
```

## Relationship between resolution and penalty

For the default binary encoding of a continuous slack on `[0, S]` with `n` bits, the grid spacing is `Delta = S / (2^n - 1)`. If a feasible source assignment requires slack `z*`, its best squared residual is

```text
p_F = min_k |z* - k Delta|^2 <= Delta^2 / 4.
```

The floor can be zero when `z*` lies on the grid; it is not strictly positive for every feasible point and need not decrease strictly at every bit count because grid alignment changes.

Penalty sizing depends on contrast, not on `Delta` alone. If an infeasible assignment has penalty `p_I` and objective advantage `B` over a feasible assignment, a positive penalty coefficient can prefer the feasible assignment only when

```text
rho (p_I - p_F) > B.
```

Thus `rho > B / (p_I - p_F)` when `p_I > p_F`. If `p_I <= p_F`, increasing `rho` cannot repair the ordering and may strengthen the wrong bias. The usual exact-penalty proof using the smallest positive penalty gap assumes feasible assignments have zero penalty; a continuous-slack floor violates that premise.

## Executable public fixture

The fixture minimizes `-(x1 + x2)` subject to `0.6(x1 + x2) <= 1` with binary source variables, `Discretize(false)`, and an explicitly bit-limited binary slack on `[0, 1]`. A one-active-variable feasible state needs slack `0.4`; the two-active-variable infeasible state has residual `0.2` and objective advantage `B = 1`.

| Bits | Grid spacing | Feasible floor `p_F` | Infeasible penalty `p_I` | Contrast | Required `rho` | Inferred `rho` | Inferred ranking |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | :--- |
| 1 | 1.0 | 0.16 | 0.04 | -0.12 | impossible | 75.0 | infeasible |
| 2 | 0.33333333 | 0.00444444 | 0.04 | 0.03555556 | 28.125 | 675.0 | feasible |
| 3 | 0.14285714 | 0.00081633 | 0.04 | 0.03918367 | 25.5208 | 3675.0 | feasible |
| 4 | 0.06666667 | 0.0 | 0.04 | 0.04 | 25.0 | 675.0 | feasible |
| 5 | 0.03225806 | 0.00016649 | 0.04 | 0.03983351 | 25.1045 | 72075.0 | feasible |
| 6 | 0.01587302 | 1.008e-5 | 0.04 | 0.03998992 | 25.0063 | 297675.0 | feasible |

At one bit, the feasible floor exceeds the infeasible penalty, so no positive `rho` can favor the feasible state. From two bits onward the contrast is positive. This is why a universal resolution-only multiplier is not an automatic remedy.

## Canonical #205 compiler audit

The canonical model has 36 source variables, all binary. ToQUBO reports `Discretize() == true`; every generated slack expansion has integral coefficients. The reported c2-c4 families (`level_select`, `logic`, and `logic_t1`) are equalities and generate no slack at all.

| Constraint family | Source constraints | Encoded penalties | Generated slacks | Slack bits | Integral expansion |
| :--- | ---: | ---: | ---: | :--- | :--- |
| `demand` | 3 | 3 | 3 | 6 | yes |
| `level_select` | 6 | 6 | 0 | - | n/a |
| `logic` | 4 | 4 | 0 | - | n/a |
| `logic_t1` | 2 | 2 | 0 | - | n/a |
| `min_down` | 2 | 2 | 2 | 1 | yes |
| `min_up` | 2 | 2 | 2 | 1 | yes |
| `ramp_down` | 4 | 4 | 4 | 5-6 | yes |
| `ramp_down_t1` | 2 | 1 | 1 | 6 | yes |
| `ramp_up` | 4 | 4 | 4 | 5-6 | yes |
| `ramp_up_t1` | 2 | 2 | 2 | 3 | yes |

One `ramp_down_t1` constraint is detected as always feasible and omitted, which explains the 1-of-2 encoded count. The remaining inequality slacks are integer encodings, so the continuous-slack residual floor is absent model-wide at this commit.

## Canonical #205 Neal sweep

Each row aggregates 100,000 returned reads. Rates are read-weighted source-feasibility rates, not only the best result from each run.

The canonical model was compiled without an internal solver, converted with `QUBOTools.ising`, and sampled with a local sparse dimod BQM using the same interaction ordering and Neal parameters as the DWave.jl wrapper.

| Configuration | c2-c4 penalty | Other penalty | Feasible | Demand violated | Level-select violated | Logic violated | Min-down violated |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| default | 26101.0 | 26101.0 | 0.0% | 6.671% | 99.92% | 95.144% | 87.011% |
| c2-c4 scale 100 | 2.6101e6 | 26101.0 | 0.102% | 29.493% | 60.835% | 0.023% | 97.124% |
| c2-c4 scale 400 | 1.04404e7 | 26101.0 | 0.156% | 53.475% | 3.96% | 0.0% | 96.308% |
| c2-c4 hint 1e7 | 1.0e7 | 26101.0 | 0.141% | 53.05% | 4.322% | 0.0% | 96.395% |
| inequalities scale 400 | 26101.0 | 1.04404e7 | 0.0% | 6.501% | 99.963% | 97.478% | 84.63% |

Full family-level violation rates:

| Configuration | Logic t1 | Min-up | Ramp-down | Ramp-down t1 | Ramp-up | Ramp-up t1 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: |
| default | 55.994% | 17.686% | 13.516% | 0.032% | 23.202% | 71.383% |
| c2-c4 scale 100 | 0.0% | 1.698% | 16.468% | 0.002% | 22.597% | 68.441% |
| c2-c4 scale 400 | 0.0% | 3.197% | 14.863% | 0.275% | 23.716% | 59.002% |
| c2-c4 hint 1e7 | 0.0% | 3.017% | 14.555% | 0.247% | 23.533% | 59.103% |
| inequalities scale 400 | 72.713% | 31.417% | 13.454% | 0.073% | 23.057% | 69.182% |

## Conclusions

- The continuous-slack mechanism is real in the public `Discretize(false)` fixture, but the relevant weight is controlled by penalty contrast, not grid spacing alone.
- The mechanism does not explain #205. Its most-violated c2-c4 constraints have no slack, and its inequality slacks are integral under the default discretization path.
- Increasing c2-c4 pressure changes the finite-run sampler landscape as reported: scale 400 reduces level-selection violations from 99.92% to 3.96% and produces a 0.156% feasible-read rate, close to the fixed `1e7` result of 0.141%.
- Scaling only the slack-bearing inequalities does not produce a feasible read and does not repair c2-c4. This is the opposite of the prediction from a slack-resolution root cause.
- The evidence does not justify a resolution-aware automatic penalty implementation for #205. Any general continuous-slack policy should be evaluated separately, and a tolerance-band design must account for the nonzero-floor failure of the usual exact-penalty premise.

## Limitations and next decision

The public fixture covers the default binary continuous encoding, not every encoding method. The canonical solver sweep measures one seeded Neal schedule and should not be read as an exact-solver guarantee. The access-controlled source is intentionally absent from this repository; collaborators need access to the pinned model commit to rerun that portion.

A follow-up documentation PR is useful to make the `Discretize()` boundary and the penalty-contrast condition explicit. A compiler PR should not be opened from this result without a separate design decision and evidence from genuinely continuous-slack models.
